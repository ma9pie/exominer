import React from "react";

import { Ores } from "@/components/home";
import { Layout } from "@/components/layout";

const Home = () => {
  return (
    <Layout>
      <Ores></Ores>
    </Layout>
  );
};

export default Home;
